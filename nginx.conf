server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Compresión gzip para assets estáticos
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml image/svg+xml;

    # Proxy hacia el backend
    location /api/ {
        proxy_pass         http://backend:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        # El navegador habla con Traefik por HTTPS y Traefik con nosotros por HTTP.
        # Sin propagar el esquema original, el backend cree que la peticion vino en
        # claro y arma mal cualquier URL absoluta que devuelva.
        proxy_set_header   X-Forwarded-Proto $http_x_forwarded_proto;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;

        # Subida de documentos. El backend acepta hasta 55MB
        # (spring.servlet.multipart.max-request-size); sin esta directiva nginx
        # aplica su default de 1m y corta con 413 antes de llegar a Spring.
        client_max_body_size 55m;
        client_body_timeout  300s;
        proxy_request_buffering off;
    }

    # SPA: todas las rutas caen a index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache agresivo para assets con hash en el nombre (Vite genera hashes)
    location ~* \.(js|css|woff2?|ttf|eot|svg|png|jpg|jpeg|gif|ico|webp)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
